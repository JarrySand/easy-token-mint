// Type declarations for Hardhat artifacts

declare module '../../artifacts/contracts/MintableToken.sol/MintableToken.json' {
  const artifact: {
    abi: unknown[];
    bytecode: string;
  };
  export default artifact;
}

declare module '../../artifacts/contracts/BatchMinter.sol/BatchMinter.json' {
  const artifact: {
    abi: unknown[];
    bytecode: string;
  };
  export default artifact;
}
